/** @type {import('next').NextConfig} */
const nextConfig = {
  // Eventuelt kan du legge til konfigurasjon for eksterne bilder her:
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
